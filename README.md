# Dropsource Javascript Challenge

Small application that renders a page and its elements from a JSON web service. Also allows users to select the individual elements. All code is located in index.html.